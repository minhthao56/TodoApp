interface ITitleHome {
  title?: string;
}
// Check Box
interface ICheckBox {
  isChecked?: boolean;
}

interface IText {
  styleText?: Object;
  children: any;
}

// ITextInputCommon
interface ITextInputCommon {
  multiline?: boolean;
  numberOfLines?: number;
  style?: Object;
  placeholder?: string;
}
// TitleAuth
interface ITitleAuth {
  title?: string;
  subTitle?: string;
}
