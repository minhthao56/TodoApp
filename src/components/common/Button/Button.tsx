import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

export const Button = () => {
  return (
    <View>
      <Text>Button</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
